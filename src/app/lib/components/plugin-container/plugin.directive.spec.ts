import { PluginDirective } from './plugin.directive';

describe('PluginDirective', () => {
  it('should create an instance', () => {
    const directive = new PluginDirective();
    expect(directive).toBeTruthy();
  });
});

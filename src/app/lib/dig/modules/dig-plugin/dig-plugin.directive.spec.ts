import { DigPluginDirective } from './dig-plugin.directive';

describe('DigPluginDirective', () => {
  it('should create an instance', () => {
    const directive = new DigPluginDirective();
    expect(directive).toBeTruthy();
  });
});
